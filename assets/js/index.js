var typed = new Typed('.typed', {
    strings: ["Меня зовут Жамшид", "Я Front-End программист"],
    // Optionally use an HTML element to grab strings from (must wrap each string in a <p>)
          stringsElement: null,
          // typing speed
          typeSpeed: 90,
          // time before typing starts
          startDelay: 1200,
          // backspacing speed
          backSpeed: 20,
          // time before backspacing
          backDelay: 500,
          loop: true,
          cursorChar: "|",
  });
  
